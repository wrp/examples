
To reload a module in interactive mode:

# python 3.4+
import importlib
importlib.reload(name)  # name is name as imported, so either the module name
                        # or the xxx from "import as xxx"
