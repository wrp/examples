See sample.service for a sample service file

systemctl is-active ${service?}                 # Check if a service is running
systemctl list-units --type=service             # List all services
systemctl enable ${service?}                    # Ensure a service starts on boot
systemctl show ${service?}                      # Show details for $service
