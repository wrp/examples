

# Select all vms with a given network tag:
$ gcloud compute instances list --filter="(tags.items ~ ${tag_regex})"

# Select all images from a given family
$ gcloud compute images list  --filter family=$family
