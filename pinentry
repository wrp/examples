
This was Claude's attempt to recreate pin-entry.  Do not use it.  Just do

# Downloaded and built official pinentry-tty
cd ~/tgz
curl -L https://www.gnupg.org/ftp/gcrypt/pinentry/pinentry-1.3.1.tar.bz2 -o pinentry-1.3.1.tar.bz2

cd ~/src
tar xjf ~/tgz/pinentry-1.3.1.tar.bz2
cd pinentry-1.3.1
./configure --enable-pinentry-tty --disable-pinentry-gtk2 --disable-pinentry-gnome3 --disable-pinentry-qt --disable-pinentry-qt5 2>&1 | tail -30

# Curses version failed to build, built TTY version only
cd ~/src/pinentry-1.3.1/tty
make
make install

# Updated ~/.gnupg/gpg-agent.conf:
# pinentry-program /Users/wrp/x86_64/Darwin/bin/pinentry-tty
# default-cache-ttl 3600
# max-cache-ttl 3600

# Restarted GPG agent
gpgconf --kill gpg-agent

# Success - pinentry-tty now works with 1-hour caching
