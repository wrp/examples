
Global gitignore is core.excludesFile in the user’s ~/.gitconfig, defaulting to
${XDG_CONFIG_HOME:-$HOME/.config}/git/ignore.


# Find all extant files that are being ignored
find . -print0 | git check-ignore -z --stdin | tr '\0' \\n
