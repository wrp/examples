A hodge-podge of things that don't have a home elsewhere but want to be
discoverable via "git grep"

--orphan is an option to git-checkout, not git-branch


git log --format=%H --all --find-object=$oid   #  Find which commits contain a blob


e69de29bb2d1d6434b8b29ae775ad8c2e48c5391 is the oid of the empty blob (empty file)
