
# For some absurd reason, --name-only/--name-status does not only show
# names, but also shows the log message.  To get a list of names only, do:
git show --pretty=format: --name-only $commitish  # See gitrevisions(7)
# -- or --
git show --pretty=format: --name-status $commitish
