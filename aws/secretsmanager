
aws secretsmanager list-secrets | jq -r .'SecretList[].Name'   # list secrets

aws secretsmanager get-secret-value \
	--secret-id ${name?} \
	--region ${region-us-west-2} \
	--profile ${profile?} \
	--query SecretString \
	--output text
