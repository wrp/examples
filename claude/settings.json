{
  "model": "claude-opus-4-6",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '\"#\" + (now | floor | tostring) + \" (\" + (now | strflocaltime(\"%Y-%m-%dT%H:%M:%S%z\")) + \" \" + .session_id[:8] + \":\" + .cwd + \")\\n\" + .tool_input.command' >> ~/.claude/bash_history.log"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "~/.claude/statusline"
  },
  "theme": "dark",
  "editorMode": "vim",
  "comment": "Put this in $HOME/.claude/settings.json",
  "tui": "fullscreen"
}
