# Install $pkg
pacman -S $pkg   # often want to use -y to refresh

# Which package owns a file:
pacman -Qo $path

# Search available packages that match $pattern
pacman -Ss "$pattern"  # pattern is an re, not a glob
