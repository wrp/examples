
# Which package provides /bin/foo?
yum provides foo

# Find which package provides /usr/bin/foo
yum whatprovides */foo

# List all packages provided by an rpm.
rpm -ql ${package}

# Which package owns /p/a/t/h
rpm -qf /p/a/t/h
